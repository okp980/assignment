import { FormikProps, FormikFormProps } from "formik"
import React from "react"
import { Values } from "../../types/form-types"

type Props = {
  children: React.ReactElement
}

const FormStep = ({ children }: Props & any) => {
  return children
}

export default FormStep
